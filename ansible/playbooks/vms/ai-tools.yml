---
- name: "Configure ai-tools VM"
  hosts: ai-tools
  gather_facts: true
  become: true
  roles:
    - geerlingguy.docker
    - crw
    - role: mcp_servers
      tags: mcp_servers_install
    - role: searxng
      tags: searxng_install
    - role: camofox
      tags: camofox_install

- name: Configure Tailscale VPN gateway
  hosts: tailscale
  become: true
  gather_facts: true
  tasks:
    - name: Playbook not yet implemented
      ansible.builtin.debug:
        msg: "WARNING: tailscale.yml is a stub — no configuration tasks defined yet."
